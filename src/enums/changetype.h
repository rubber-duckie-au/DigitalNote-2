#ifndef ENUM_CHANGETYPE_H
#define ENUM_CHANGETYPE_H

/** General change type (added, updated, removed). */
enum ChangeType
{
    CT_NEW,
    CT_UPDATED,
    CT_DELETED
};

#endif // ENUM_CHANGETYPE_H
